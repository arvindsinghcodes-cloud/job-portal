#services:
#  dbservice:
#    image: mysql:latest
#    container_name: jobportaldb
#    ports:
#     - "3306:3306"
#    environment:
#      MYSQL_ROOT_PASSWORD: root
#      MYSQL_DATABASE: jobportal
#    volumes:
#      - /home/arvind-ksingh/Documents/job-portal-data:/var/lib/mysql
#    tmpfs:
#      - /var/run/mysqld


services:
  dbservice:
    image: mysql:8.4
    container_name: jobportaldb
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: jobportal
    volumes:
      - mysql_data:/var/lib/mysql
  grafana-lgtm:
    image: 'grafana/otel-lgtm:latest'
    ports:
      - '3000:3000' # Grafana UI
      - '4317:4317' # OpenTelemetry gRPC port
      - '4318:4318' # OpenTelemetry HTTP port

volumes:
  mysql_data: